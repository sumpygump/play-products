# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="upxIvn0SvpMrKL7ce<q7s6_]kUrRB9sZeDB5^9rBuN2eyOsm8XcS;H1OybbHEc]t"

# The application languages
# ~~~~~
application.langs="en,es,fr,nl"

# Application revision
# I wanted a way to keep track of the current version of the codebase
# Note: play has this 'version' setting that maybe could be used instead
application.revision="0.1.6"

# Database configuration
db.default.driver="org.h2.Driver"
db.default.url="jdbc:h2:mem:play"

# Reports who the user is who invoked the play server
environment.user=${USER}
